
uttrekk1 is an example of an extraction where everything is meant to validate. Not there yet.

uttrekk2 is an example of an extraction that contains every noark 5 complexType and simpleType,
but contains errors. This extraction should have an example of every error the validator checks
for

uttrekk3 (not ready yet) is an example of an extraction that contains every noark 5 complexType
and simpleType, and contains no errors.